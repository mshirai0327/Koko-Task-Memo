# きえもの — 消える ToDo アプリ 設計・引き継ぎ仕様書

> **v0.1 PROTOTYPE** / フロントのみ / 個人利用

| 項目 | 内容 |
|------|------|
| プロジェクト名 | Koko-Task |
| コンセプト | 72時間で消える、軽くて楽しいパーソナルToDoアプリ |
| 現フェーズ | フロントのみ（HTML/CSS/JS + localStorage） |
| ターゲット | 個人利用・プライベート。チーム管理ツールではない |
| ステータス | プロトタイプ完成。次フェーズ検討中 |

---

## 1. コンセプトと設計哲学

### 核心思想

「**忘れてもいい**」を仕様にする。タスクは72時間後に自動削除され、管理しなくてよいことが前提。Jira・ClickUp のような重厚なタスク管理ではなく、付箋や頭の中のメモに近い感覚を目指す。

### 設計原則（優先順位順）

1. **触って楽しいこと** — CSSアニメーション豊か、インタラクションが気持ちいい
2. **シンプルであること** — 機能は最小限。設定画面はできるだけ作らない
3. **軽くてよいこと** — 忘れたタスクは消えてよい。完璧な記録は不要
4. **どこからでも使えること** — PC・スマホ両対応。ブラウザアクセス必須
5. **親しみがあること** — 日本語UI、柔らかいフォント、感情のあるコピー

### アンチパターン（やらないこと）

- タスクの優先度・ラベル・カテゴリ管理
- リマインダー・通知機能（将来的にはあってもいい）
- チームシェア・コラボレーション機能
- 複雑な設定UI
- タスクの無限保存・アーカイブ

---

## 2. 機能仕様

### 2-1. タスク追加

- テキスト入力 → Enterキー または「+」ボタンで追加
- 最大文字数: 120文字
- 追加時: バウンスアニメーション付きでカードがスライドイン
- 追加と同時に `createdAt` タイムスタンプを記録（TTL計算の基準）

### 2-2. タスク完了

- チェックボタンをクリックでトグル
- 完了状態: 打ち消し線・透明度低下・緑チェック
- 完了タスクは「おわった」セクションに移動
- 完了しても72時間TTLは継続（完了しても消える）

### 2-3. タスク編集

- タスクテキストをクリックで `contenteditable` 編集
- Enterキーまたはフォーカスアウトで確定
- 空文字での確定は不可（元のテキストに戻す）

### 2-4. タスク削除

- ✕ボタンクリックで vanish アニメーション → DOM削除
- TTL切れによる自動削除も同様のアニメーション

### 2-5. TTL（72時間自動削除）

- 各タスクに `createdAt` を保持
- 残り時間をカード右端に表示（例: `48h 30m` / `2m 10s`）
- 残り時間30%以下で `--accent2`（ピンク）色に変化
- カード下部にTTLバー（残り時間比率で縮む）
- 毎分チェック。切れたものを localStorage から除去 + アニメーション削除

---

## 3. データ構造

```js
// localStorage キー
const STORAGE_KEY = 'kiemono_tasks_v1';

// タスクオブジェクト
{
  id: string,        // Date.now().toString(36) + random
  text: string,      // タスクテキスト（最大120文字）
  createdAt: number, // Unix ms タイムスタンプ
  done: boolean,     // 完了フラグ
}
```

- ストレージキーのバージョニング（v1, v2...）でマイグレーション対応
- 起動時に期限切れタスクをフィルタリングして保存

---

## 4. デザインシステム

### カラーパレット

| 変数 | 値 | 用途 |
|------|----|------|
| `--bg` | `#0F0E17` | 背景（ほぼ黒） |
| `--surface` | `#1A1828` | カード背景 |
| `--surface2` | `#242136` | ホバー時 |
| `--ink` | `#FFFFFE` | メインテキスト |
| `--ink-dim` | `#A7A5BC` | サブテキスト |
| `--ink-ghost` | `#4A4862` | ヒント・ラベル |
| `--accent` | `#FF8906` | オレンジ（メインアクション） |
| `--accent2` | `#E53170` | ピンク（緊急・削除） |
| `--accent3` | `#3DA9FC` | ブルー（TTLバー） |
| `--done` | `#2CB67D` | 完了（グリーン） |

### タイポグラフィ

| 変数 | フォント | 用途 |
|------|---------|------|
| `--font-hand` | Klee One | ロゴ・プレースホルダー・空状態テキスト |
| `--font-ja` | Zen Kaku Gothic New | 本文・タスクテキスト（weight 300/400/700） |
| `--font-mono` | DM Mono | TTLラベル・セクションヘッダー・フッター |

### レイアウト

- 最大幅 560px、中央寄せ
- padding: `48px 20px 120px`（フッターとの重なり回避）
- モバイル（≤480px）: ロゴ 1.8rem、上部パディング 32px

### アニメーション仕様

| 名前 | 内容 |
|------|------|
| `cardIn` | 追加時: Y軸-12px + scale(0.96) → 通常。`cubic-bezier(0.34,1.4,0.64,1)` バウンス |
| `vanish` | 削除時: opacity+scale縮小 → max-height:0 でスペースも消える。0.6s |
| `fadeSlideDown` | ヘッダー・入力欄の初期表示。0.6s ease |
| `float` | 空状態アイコンの浮遊。3s ease-in-out infinite |
| `drift` | 背景グロウの揺れ。20s infinite。3つの光が異なるdelay |
| TTLバー | width: CSSトランジション 1s linear で縮む |

---

## 5. 技術構成（現フェーズ）

| 項目 | 内容 |
|------|------|
| フロントエンド | Vanilla HTML/CSS/JS（フレームワークなし） |
| スタイル | CSS変数 + カスタムアニメーション |
| フォント | Google Fonts CDN |
| ストレージ | localStorage（デバイス間同期なし） |
| ビルドツール | なし（単一HTMLファイル） |
| デプロイ | 静的ホスティング（GitHub Pages / Cloudflare Pages など） |

---

## 6. 次フェーズ候補

### Phase A: PWA化（コスト: 低）

- `manifest.json` + service worker 追加
- スマホのホーム画面にインストール可能
- オフライン動作
- デバイス間同期は依然なし

### Phase B: 軽量バックエンド + デバイス間同期（コスト: 低〜中）

| 項目 | 内容 |
|------|------|
| フロント | React + Vite（既存デザイン踏襲） |
| バックエンド | Cloudflare Workers + KV（ほぼ無料） |
| 認証 | 匿名セッション（UUIDクッキー）または Magic Link |
| 同期 | WebSocket or polling |
| TTL管理 | KVのexpiration機能で自動削除も可能 |

### Phase C: ネイティブアプリ展開（コスト: 中）

- Tauri（Rust製ランタイム）でWeb→デスクトップアプリ化
- Capacitor でモバイルアプリ（iOS/Android）化
- WebのコードをほぼそのままReuse可能

### Phase D: 軽量ソーシャル機能（将来構想）

- タスクを「今日だけ公開」できる機能
- 見知らぬ人のタスクを眺められるタイムライン（DRIFT的思想との接続）
- 完了数・ストリーク等のゆるい記録

---

## 7. 既知の制約・TODO

### 制約

- localStorage はデバイス固有。別デバイスからは見えない
- localStorage の容量上限（通常5MB）
- `contenteditable` 編集はIMEで稀に二重入力が起きる可能性あり
- TTL更新が1分間隔なので残り時間表示が最大1分ずれる

### TODO / 改善候補

- [ ] Enterキーでの日本語入力確定時の誤送信対策（`isComposing` チェック）
- [ ] タスクの並び替え（ドラッグ&ドロップ）
- [ ] タスク数が増えた時の仮想スクロール検討
- [ ] ライトテーマ対応
- [ ] PWA化: manifest.json, service worker
- [ ] アクセシビリティ: aria属性, フォーカス管理

---

## 8. Claude への引き継ぎプロンプト

新しいチャットで以下をそのまま貼り付けて、最終行だけ書き換えて使ってください。

---

```
私は「きえもの」という個人用ToDoアプリを開発しています。
以下の仕様を引き継いで作業を継続してください。

■ コンセプト
- 72時間でタスクが自動削除される「消えること前提」のToDoアプリ
- 管理ではなく「メモより少し整理された記録」という位置づけ
- CSSアニメーションが豊かで触って楽しいUI
- シンプル・最小機能。設定画面なし

■ 現状
- フロントのみ（単一HTML）でプロトタイプ完成
- localStorage で永続化（デバイス間同期なし）
- 機能: 追加 / 完了 / 編集 / 削除 / TTL表示 / 自動削除

■ デザイン
- ダークテーマ: bg #0F0E17, accent #FF8906, accent2 #E53170
- フォント: Klee One（ロゴ）/ Zen Kaku Gothic New（本文）/ DM Mono（ラベル）
- アニメーション: cardIn（バウンス）/ vanish（消滅）/ drift（背景グロウ）

■ タスクデータ構造
{ id: string, text: string, createdAt: number, done: boolean }
localStorage key: 'kiemono_tasks_v1'

■ 次フェーズ候補
A: PWA化（manifest + service worker）
B: Cloudflare Workers + KV でデバイス間同期
C: Tauri / Capacitor でネイティブアプリ化

今回お願いしたいこと: [ここに具体的な作業を書く]
```

---

*きえもの — 軽くてよい。忘れてもよい。*